[package]
name = "account-message-store"
description = "Bounded per-account OneBot message state for Lirvena."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
account-runtime = { version = "=0.0.0", path = "../account-runtime" }
local-state = { version = "=0.0.0", path = "../local-state" }
rusqlite = { version = "=0.40.2", default-features = false, features = ["bundled"] }
serde_json = "=1.0.151"

[dev-dependencies]
tempfile = "=3.27.0"

[lints]
workspace = true
